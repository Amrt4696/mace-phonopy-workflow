# This example is meant to be regenerated by run_example.sh on every clone,
# so its phonopy outputs are not committed (unlike examples/bridgmanite-MgSiO3).
POSCAR-0*
SPOSCAR
phonopy_disp.yaml
phonopy.yaml
FORCE_SETS
FORCE_CONSTANTS
band.yaml
band.dat
band.pdf
